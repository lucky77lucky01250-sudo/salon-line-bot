# 進捗報告 2026-07-19

## やったこと（段階3: 管理画面+お知らせ一斉配信）
- `/admin` 配下をBasic認証で保護（`proxy.ts`。Next.js 16ではmiddlewareがproxyに改称）。パスワードは env `ADMIN_PASSWORD`（.env.localとVercelに設定済み）。未設定時は全拒否（fail closed）
- 管理画面3ページを実装（スマホ375px基準・タップ領域44px以上・専門用語なし・処理中スピナー）
  - `/admin/faq`: よくある質問の追加・編集・削除（Server Actions）
  - `/admin/menus`: メニューと料金の追加・編集・削除
  - `/admin/broadcast`: お知らせ配信。「自分にテスト送信」→確認→「全員に配信」の2段階+確認ダイアログで誤配信防止。無料プラン月200通の注意書きあり
- 編集内容は即DBに反映され、次のLINE自動応答から使われる（管理画面→bot連携は追加作業なし）

## 検証
- ローカル: Basic認証401/200、FAQ・メニューのCRUDをServer Action直POSTでE2E確認（DB反映まで）
- 本番: 認証なし401 / 認証あり200（3ページとも）、Webhookリグレッション（FAQ質問→high回答→ログ保存）確認
- 配信ボタンの実送信は実機UAT待ち（push/broadcast API自体は段階2で実証済み）

## トラブルと対処
- 初回アクセスのみSupabaseが `PGRST303 JWT issued at future` を返す事象 → 実体は「過去に発生した瞬間的な時計ズレ応答が `.next` のdevキャッシュに残り再生されていた」。対処: Supabaseクライアントのfetchを `cache: "no-store"` に固定+PGRST303時のみ1回リトライ

## 実機UAT（完了）
- スマホで管理画面にログインし、以下4点すべて確認済み
  1. エスカレーション通知がオーナーLINEに届く
  2. 管理画面でFAQの営業時間を編集→botの回答が即座に変わる（9:00→10:00で確認）
  3. お知らせ「自分にテスト送信」が届く
  4. 「全員に配信」が届く（配信前にInsight APIで友だち数=1を確認してから実施）
- **これで段階3完了**

## 次にやること
- 段階4: 統合テスト→納品準備（README・引き渡しドキュメント）

## 使用したClaude Code機能
- Server Actionのプログレッシブエンハンスメント経路（$ACTION_ID直POST）によるブラウザなしE2Eテスト
- ブラウザ操作でVercel環境変数追加+再デプロイ
